[project]
name = "ug-timetaggermodule"
version = "1.0.2"
description = "User Guide of cronologic's TimeTagger Module"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "furo>=2025.12.19",
    "sphinx>=9.1.0",
]

[dependency-groups]
dev = [
    "esbonio>=2.1.0",
]
